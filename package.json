{
  "name": "ucheba-3",
  "version": "1.0.0",
  "private": true,
  "description": "Tiny Three.js envelope demo",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && npx gh-pages -d dist"
  },
  "dependencies": {
    "three": "^0.181.0"
  },
  "devDependencies": {
    "vite": "^7.2.2"
  }
}
